#ifndef GREET_H
#define GREET_H

/* Declaration only: the header promises this function exists somewhere.
   The include guard above ensures this is processed at most once per file. */
void greet(const char *name);

#endif /* GREET_H */
