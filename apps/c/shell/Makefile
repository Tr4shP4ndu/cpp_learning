# Built by the workspace — from the repo root:  make run-c-app-<this-folder>
# The root Makefile exports CC/CFLAGS and passes BUILD_DIR + OUTPUT_NAME in.
all:
	$(CC) $(CFLAGS) -Iinclude -Isrc src/*.c -o $(BUILD_DIR)/$(OUTPUT_NAME)
