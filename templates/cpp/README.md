# App

## What it does
<!-- One or two plain-English sentences. -->

## Build & run
```sh
# from the repo root (NAME = this folder's name)
make run-cpp-app-NAME
make run-cpp-app-NAME STD=c++98      # try an older standard
make run-cpp-app-NAME BUILD_TYPE=Release
```
